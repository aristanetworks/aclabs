! Startup-config last modified at Fri Feb 20 14:00:54 2026 by root
! device: EOS2 (cEOSLab, EOS-4.35.1F-45295350.4351F.1 (engineering build))
!
no aaa root
!
username admin privilege 15 role network-admin secret sha512 $6$JrKr5rmBWqhG.S3c$tA4xefxaSOYDKw7ujFe1SP/C38Ra0qFmcwWI9fwQ.3mCA/gU0qTjYNXm3A5LcATrKh0EjjW6z3irWZ6aI6V031
!
management api http-commands
   no shutdown
   !
   vrf MGMT
      no shutdown
!
no service interface inactive port-id allocation disabled
!
transceiver qsfp default-mode 4x10G
!
interface defaults
   mtu 9214
!
service routing protocols model multi-agent
!
hostname EOS2
!
spanning-tree mode mstp
!
system l1
   unsupported speed action error
   unsupported error-correction action error
!
vrf instance MGMT
!
management api gnmi
   transport grpc default
      vrf MGMT
!
management api netconf
   transport ssh default
      vrf MGMT
!
aaa authorization exec default local
!
interface Ethernet1
   no switchport
   ip address unnumbered Loopback0
   isis enable 100
   isis circuit-type level-2
   isis network point-to-point
!
interface Ethernet2
   no switchport
   ip address unnumbered Loopback0
   isis enable 100
   isis circuit-type level-2
   isis network point-to-point
!
interface Ethernet3
   no switchport
   ip address unnumbered Loopback0
   isis enable 100
   isis circuit-type level-2
   isis network point-to-point
!
interface Ethernet4
   no switchport
!
interface Loopback0
   ip address 100.0.0.2/32
   node-segment ipv4 index 2
   isis enable 100
   isis circuit-type level-2
   isis passive
!
interface Management0
   vrf MGMT
   ip address 172.100.100.102/24
!
ip routing
no ip routing vrf MGMT
!
ipv6 unicast-routing
!
ip route vrf MGMT 0.0.0.0/0 172.100.100.1
!
mpls ip
!
router bfd
   interval 500 min-rx 500 multiplier 3 default
!
router bgp 100
   router-id 100.0.0.2
   no bgp default ipv4-unicast
   distance bgp 20 200 200
   neighbor default send-community
   neighbor RR peer group
   neighbor RR remote-as 100
   neighbor RR update-source Loopback0
   neighbor 20.0.0.4 remote-as 501
   neighbor 20.0.0.4 description EOS7
   neighbor 20.0.0.4 ebgp-multihop 5
   neighbor 20.0.0.4 timers 1 3
   neighbor 100.0.0.4 peer group RR
   neighbor 100.0.0.4 description EOS4
   !
   address-family evpn
      neighbor default encapsulation mpls next-hop-self source-interface Loopback0
      neighbor RR activate
   !
   address-family ipv6
      neighbor RR activate 6pe
!
router isis 100
   net 49.1111.0000.0000.0002.00
   router-id ipv4 100.0.0.2
   is-type level-2
   !
   address-family ipv4 unicast
   !
   segment-routing mpls
      no shutdown
!
router multicast
   ipv4
      software-forwarding kernel
   !
   ipv6
      software-forwarding kernel
!
end
