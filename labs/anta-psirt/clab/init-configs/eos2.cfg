no aaa root
!
username arista privilege 15 role network-admin secret arista
!
vrf instance MGMT
!
service routing protocols model multi-agent
!
management security
   ssl profile psirt-mtls
      !! VerifySA146 support: this valid profile provides mTLS evidence for gNMI.
      certificate node.crt key node.key
      trust certificate ca.crt
!
management api http-commands
   protocol https
   no shutdown
   !
   vrf MGMT
      no shutdown
!
management api gnmi
   !! VerifySA117 expected: Inconclusive - accounting is enabled.
   !! VerifySA146 expected: Inconclusive (Mitigated) - mTLS covers gNMI.
   transport grpc default
      ssl profile psirt-mtls
      accounting requests
      authentication username priority x509-common-name
      no shutdown
!
management ssh
   !! VerifySA147 expected: Failure (Affected) - SSH server is affected; the critical client path remains Inconclusive without strict checking.
!
aaa authorization exec default local
!
hostname eos2
!
spanning-tree mode mstp
!
interface Ethernet1
!
interface Ethernet2
!
interface Ethernet3
!
interface Ethernet4
!
interface Management1
   vrf MGMT
   ip address 172.100.100.12/24
!
ip route vrf MGMT 0.0.0.0/0 172.100.100.1
!
end
