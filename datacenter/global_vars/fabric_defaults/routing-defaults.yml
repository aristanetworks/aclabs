---
bgp_graceful_restart:
  enabled: true
  restart_time: 300

bgp_distance:
  external_routes: 20
  internal_routes: 200
  local_routes: 200

default_evpn_encapsulation: vxlan

evpn_import_pruning: true

mlag_ibgp_peering_vrfs:
  base_vlan: 3000
